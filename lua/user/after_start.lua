-- AFTER START-------------------------------------
-- Commands to run after neovim is started --------
---------------------------------------------------

-- Autoload session, first uncomment that:
-- require('persistence').load()
--
-- Go to your plugins with SPC f p and add:
-- { 'goolord/alpha-nvim', enabled = false }
-- inside user_plugins
vim.g.format_on_save = false -- To configure your formatters, press SPC f p and find LSP.  SPC h f to enable/disable format on save.
